{
  "name": "go-asistencias-trabajadores",
  "version": "1.0.0",
  "description": "Sistema de gestión de asistencias y trabajadores",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}